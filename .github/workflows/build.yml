name: 打包Windows应用

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 设置Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 添加msbuild到PATH
      uses: microsoft/setup-msbuild@v1.1

    - name: 安装依赖
      run: npm install

    - name: 重新编译原生模块
      run: npx electron-rebuild -f -w better-sqlite3,node-window-manager,extract-file-icon

    - name: TypeScript编译
      run: npx tsc

    - name: Vite构建
      run: npx vite build

    - name: 打包Windows应用
      run: npx electron-builder --win
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 上传exe文件
      uses: actions/upload-artifact@v4
      with:
        name: WeFlow-Windows-Setup
        path: release/*.exe
        retention-days: 30
